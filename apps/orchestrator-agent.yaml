apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: orchestrator-agent
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: https://github.com/kle04/manifest-llmops-multi-agents
    targetRevision: main
    path: charts/orchestrator-agent
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: orchestrator-agent
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
